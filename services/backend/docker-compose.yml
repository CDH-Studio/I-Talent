version: "3"
services:
  db-postgres:
    container_name: "upskill-test-database"
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: api
      POSTGRES_USER: api
      POSTGRES_DB: testdb
    ports:
      - "5432:6666"
